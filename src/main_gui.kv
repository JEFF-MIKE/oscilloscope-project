#:kivy 2.3.0

<RV>:
    viewclass: 'LabelledButton'
    RecycleBoxLayout:
        default_size: dp(25), dp(56)
        default_size_hint: 1, None
        size_hint_y: None
        height: self.minimum_height
        orientation: 'vertical'
        align: 'right'